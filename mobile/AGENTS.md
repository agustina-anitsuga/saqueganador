# Expo SDK 54

This project targets **Expo SDK 54** (pinned to match the user's Expo Go, which supports SDK 54).
Read the versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing code.
Do not upgrade the SDK unless the user's Expo Go supports the newer version.
